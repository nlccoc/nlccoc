nlccapp.run(["$rootScope", "$state", function($rootScope, $state) {
  console.log("nlcc app run");
  $rootScope.loaded = true; // state to be accessed from view
  //$rootScope.$settings = settings; // state to be accessed from view
}]);

//nlccext.run(["$rootScope", "$state", function($rootScope, $state) {
  //console.log("nlcc app run");
  //$rootScope.loaded = true; // state to be accessed from view
  //$rootScope.$settings = settings; // state to be accessed from view
//}]);
